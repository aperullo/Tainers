[tool.poetry]
name = "tainers"
version = "0.0.1"
description = "Simple replacement for testcontainers-python"
authors = ["ap <18688190+aperullo@users.noreply.github.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
docker = "^5.0.3"

[tool.poetry.dev-dependencies]
black = "^22.6.0"
pytest = "^7.1.2"
pytest-cov = "^3.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
